Dragon House background assets

- `login-portal-background.png` is the optional texture/image layer inside the login arch portal.
- `post-login-background.png` is the configurable background layer for the authenticated Family Hub screen.

Replace either file with your own image using the same filename to customize the experience without changing component code.
